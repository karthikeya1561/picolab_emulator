/*
 * pico/types.h — Shared type definitions for Pico SDK simulation
 *
 * Provides common types used across all SDK headers.
 * These match the real Pico SDK type conventions.
 */

#ifndef PICO_TYPES_H
#define PICO_TYPES_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

/* Pico SDK uses 'uint' as shorthand for unsigned int */
typedef unsigned int uint;

#endif /* PICO_TYPES_H */
