---
description: how to build and run the PicoLab emulator
---

1. Ensure Emscripten is installed and activated in your terminal.
2. Run the build script to compile C code to WASM:

```powershell
./build.bat
```

3. Start the development server:

```powershell
npm run dev
```

4. Open the displayed URL in your browser.
